export default class Payroll {
    constructor(company, position, hourrate, extrahourrate) {
        this.company = company;
        this.position = position;
        this.hourrate = hourrate;
        this.extrahourrate = extrahourrate;
    }
}
